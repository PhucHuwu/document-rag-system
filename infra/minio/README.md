# MinIO

Local MinIO stores original and processed document files.

Default local credentials:

```text
Access key: minioadmin
Secret key: minioadmin
Console: http://localhost:9001
```

The default document bucket is `tina-documents`. Bucket creation can be automated later through Core Backend bootstrap or an infra init container.
